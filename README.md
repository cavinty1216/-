# JARVIS OS

개인용 대시보드 웹앱 (React + Vite, localStorage 저장)

## 로컬/Replit 실행

```bash
npm install
npm run dev
```

## 빌드 (Vercel 배포용)

```bash
npm run build
```

`dist` 폴더가 생성되면 그대로 정적 호스팅(예: Vercel)에 올리면 됩니다.
